using UnityEngine;
using System.Collections;

public class GameManager : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Cursor.lockState = CursorLockMode.Locked; // lock cursor to middle of screen on game start. (press ESC to restore cursor)
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
